#include <stdio.h>
#include "virtual_memory_manager.h"

int main() {
	printf("Hello world from Karl's virtual memory manager!\n\n");

    int response = todo();

    printf("Response: %d\n", response);

	return 0;
}
